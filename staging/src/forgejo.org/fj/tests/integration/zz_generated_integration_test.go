// Code generated by api/gen; DO NOT EDIT.

package integration

import (
	"bytes"
	"encoding/json"
	"testing"
)

// testRepo is the name of a shared repo created by setupTestRepo for
// repo-scoped commands. Created once per test run, cleaned up at exit.
var testRepo string

// TestGenerated_Activitypub tests all 11 auto-generated activitypub commands.
func TestGenerated_Activitypub(t *testing.T) {
	skipIfNoInstance(t)
	binary := buildFjBinary(t)
	t.Run("activitypubInstanceActor", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-instance-actor"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("activitypubInstanceActorInbox", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-instance-actor-inbox"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("activitypubInstanceActorOutbox", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-instance-actor-outbox"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("activitypubPerson", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-person",
			"--user-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("activitypubPersonActivity", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-person-activity",
			"--user-id", "1",
			"--activity-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("activitypubPersonActivityNote", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-person-activity-note",
			"--user-id", "1",
			"--activity-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("activitypubPersonFeed", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-person-feed",
			"--user-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("activitypubPersonInbox", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-person-inbox",
			"--user-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("activitypubRepository", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-repository",
			"--repository-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("activitypubRepositoryInbox", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-repository-inbox",
			"--repository-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("activitypubRepositoryOutbox", func(t *testing.T) {
		args := []string{"api", "activitypub", "activitypub-repository-outbox",
			"--repository-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
}

// TestGenerated_Admin tests all 51 auto-generated admin commands.
func TestGenerated_Admin(t *testing.T) {
	skipIfNoInstance(t)
	binary := buildFjBinary(t)
	t.Run("adminAddRuleToQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-add-rule-to-quota-group",
			"--quotagroup", "1",
			"--quotarule", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminAddUserToQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-add-user-to-quota-group",
			"--quotagroup", "1",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminAdoptRepository", func(t *testing.T) {
		args := []string{"api", "admin", "admin-adopt-repository",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreateHook", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-hook"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreateOrg", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-org",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreatePublicKey", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-public-key",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreateQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-quota-group"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreateQuotaRule", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-quota-rule"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreateRepo", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-repo",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreateUser", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-user"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCreateUserAccessToken", func(t *testing.T) {
		args := []string{"api", "admin", "admin-create-user-access-token",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminCronList", func(t *testing.T) {
		args := []string{"api", "admin", "admin-cron-list",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminCronRun", func(t *testing.T) {
		args := []string{"api", "admin", "admin-cron-run",
			"--task", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteHook", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-hook",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-quota-group",
			"--quotagroup", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteQuotaRule", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-quota-rule",
			"--quotarule", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteUnadoptedRepository", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-unadopted-repository",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteUser", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-user",
			"--username", testUser(),
			"--purge", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteUserAccessToken", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-user-access-token",
			"--username", testUser(),
			"--token", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteUserEmails", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-user-emails",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminDeleteUserPublicKey", func(t *testing.T) {
		args := []string{"api", "admin", "admin-delete-user-public-key",
			"--username", testUser(),
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminEditHook", func(t *testing.T) {
		args := []string{"api", "admin", "admin-edit-hook",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminEditQuotaRule", func(t *testing.T) {
		args := []string{"api", "admin", "admin-edit-quota-rule",
			"--quotarule", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminEditUser", func(t *testing.T) {
		args := []string{"api", "admin", "admin-edit-user",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminGetActionRunJobs", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-action-run-jobs",
			"--labels", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetAllEmails", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-all-emails",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetAllOrgs", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-all-orgs",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetHook", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-hook",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-quota-group",
			"--quotagroup", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetQuotaRule", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-quota-rule",
			"--quotarule", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetRegistrationToken", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-registration-token"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetRunnerRegistrationToken", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-runner-registration-token"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminGetUserQuota", func(t *testing.T) {
		args := []string{"api", "admin", "admin-get-user-quota",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminListHooks", func(t *testing.T) {
		args := []string{"api", "admin", "admin-list-hooks",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminListQuotaGroups", func(t *testing.T) {
		args := []string{"api", "admin", "admin-list-quota-groups"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminListQuotaRules", func(t *testing.T) {
		args := []string{"api", "admin", "admin-list-quota-rules"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminListUserAccessTokens", func(t *testing.T) {
		args := []string{"api", "admin", "admin-list-user-access-tokens",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminListUserEmails", func(t *testing.T) {
		args := []string{"api", "admin", "admin-list-user-emails",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminListUsersInQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-list-users-in-quota-group",
			"--quotagroup", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminRemoveRuleFromQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-remove-rule-from-quota-group",
			"--quotagroup", "1",
			"--quotarule", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminRemoveUserFromQuotaGroup", func(t *testing.T) {
		args := []string{"api", "admin", "admin-remove-user-from-quota-group",
			"--quotagroup", "1",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminRenameUser", func(t *testing.T) {
		args := []string{"api", "admin", "admin-rename-user",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminSearchEmails", func(t *testing.T) {
		args := []string{"api", "admin", "admin-search-emails",
			"--q", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminSearchRunJobs", func(t *testing.T) {
		args := []string{"api", "admin", "admin-search-run-jobs",
			"--labels", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminSearchUsers", func(t *testing.T) {
		args := []string{"api", "admin", "admin-search-users",
			"--source-id", "1",
			"--login-name", "1",
			"--is-2fa-enabled", "1",
			"--sort", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("adminSetUserQuotaGroups", func(t *testing.T) {
		args := []string{"api", "admin", "admin-set-user-quota-groups",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("adminUnadoptedList", func(t *testing.T) {
		args := []string{"api", "admin", "admin-unadopted-list",
			"--page", "1",
			"--limit", "1",
			"--pattern", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("deleteAdminRunner", func(t *testing.T) {
		args := []string{"api", "admin", "delete-admin-runner",
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("getAdminRunner", func(t *testing.T) {
		args := []string{"api", "admin", "get-admin-runner",
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getAdminRunners", func(t *testing.T) {
		args := []string{"api", "admin", "get-admin-runners",
			"--visible", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("registerAdminRunner", func(t *testing.T) {
		args := []string{"api", "admin", "register-admin-runner"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
}

// TestGenerated_Misc tests all 105 auto-generated misc commands.
func TestGenerated_Misc(t *testing.T) {
	skipIfNoInstance(t)
	binary := buildFjBinary(t)
	t.Run("createCurrentUserRepo", func(t *testing.T) {
		args := []string{"api", "misc", "create-current-user-repo"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("createOrgRepoDeprecated", func(t *testing.T) {
		args := []string{"api", "misc", "create-org-repo-deprecated",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("createUserVariable", func(t *testing.T) {
		args := []string{"api", "misc", "create-user-variable",
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deletePackage", func(t *testing.T) {
		args := []string{"api", "misc", "delete-package",
			"--owner", testUser(),
			"--type", "1",
			"--name", "1",
			"--version", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteUserRunner", func(t *testing.T) {
		args := []string{"api", "misc", "delete-user-runner",
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteUserSecret", func(t *testing.T) {
		args := []string{"api", "misc", "delete-user-secret",
			"--secretname", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteUserVariable", func(t *testing.T) {
		args := []string{"api", "misc", "delete-user-variable",
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("getActionsRun", func(t *testing.T) {
		args := []string{"api", "misc", "get-actions-run"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getGeneralAPISettings", func(t *testing.T) {
		args := []string{"api", "misc", "get-general-a-p-i-settings"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getGeneralAttachmentSettings", func(t *testing.T) {
		args := []string{"api", "misc", "get-general-attachment-settings"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getGeneralRepositorySettings", func(t *testing.T) {
		args := []string{"api", "misc", "get-general-repository-settings"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getGeneralUISettings", func(t *testing.T) {
		args := []string{"api", "misc", "get-general-u-i-settings"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getGitignoreTemplateInfo", func(t *testing.T) {
		args := []string{"api", "misc", "get-gitignore-template-info",
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getLabelTemplateInfo", func(t *testing.T) {
		args := []string{"api", "misc", "get-label-template-info",
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getLicenseTemplateInfo", func(t *testing.T) {
		args := []string{"api", "misc", "get-license-template-info",
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getNodeInfo", func(t *testing.T) {
		args := []string{"api", "misc", "get-node-info"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getPackage", func(t *testing.T) {
		args := []string{"api", "misc", "get-package",
			"--owner", testUser(),
			"--type", "1",
			"--name", "1",
			"--version", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getSSHSigningKey", func(t *testing.T) {
		args := []string{"api", "misc", "get-s-s-h-signing-key"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getSigningKey", func(t *testing.T) {
		args := []string{"api", "misc", "get-signing-key"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getUserRunner", func(t *testing.T) {
		args := []string{"api", "misc", "get-user-runner",
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getUserRunners", func(t *testing.T) {
		args := []string{"api", "misc", "get-user-runners",
			"--visible", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getUserSettings", func(t *testing.T) {
		args := []string{"api", "misc", "get-user-settings"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getUserVariable", func(t *testing.T) {
		args := []string{"api", "misc", "get-user-variable",
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getUserVariablesList", func(t *testing.T) {
		args := []string{"api", "misc", "get-user-variables-list",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getVerificationToken", func(t *testing.T) {
		args := []string{"api", "misc", "get-verification-token"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getVersion", func(t *testing.T) {
		args := []string{"api", "misc", "get-version"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("linkPackage", func(t *testing.T) {
		args := []string{"api", "misc", "link-package",
			"--owner", testUser(),
			"--type", "1",
			"--name", "1",
			"--repo-name", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("listGitignoresTemplates", func(t *testing.T) {
		args := []string{"api", "misc", "list-gitignores-templates"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("listLabelTemplates", func(t *testing.T) {
		args := []string{"api", "misc", "list-label-templates"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("listLicenseTemplates", func(t *testing.T) {
		args := []string{"api", "misc", "list-license-templates"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("listPackageFiles", func(t *testing.T) {
		args := []string{"api", "misc", "list-package-files",
			"--owner", testUser(),
			"--type", "1",
			"--name", "1",
			"--version", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("listPackages", func(t *testing.T) {
		args := []string{"api", "misc", "list-packages",
			"--owner", testUser(),
			"--page", "1",
			"--limit", "1",
			"--type", "1",
			"--q", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgAddTeamMember", func(t *testing.T) {
		args := []string{"api", "misc", "org-add-team-member",
			"--id", "1",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgAddTeamRepository", func(t *testing.T) {
		args := []string{"api", "misc", "org-add-team-repository",
			"--id", "1",
			"--org", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgDeleteTeam", func(t *testing.T) {
		args := []string{"api", "misc", "org-delete-team",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgEditTeam", func(t *testing.T) {
		args := []string{"api", "misc", "org-edit-team",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgGetTeam", func(t *testing.T) {
		args := []string{"api", "misc", "org-get-team",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListCurrentUserOrgs", func(t *testing.T) {
		args := []string{"api", "misc", "org-list-current-user-orgs",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListTeamActivityFeeds", func(t *testing.T) {
		args := []string{"api", "misc", "org-list-team-activity-feeds",
			"--id", "1",
			"--date", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListTeamMember", func(t *testing.T) {
		args := []string{"api", "misc", "org-list-team-member",
			"--id", "1",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListTeamMembers", func(t *testing.T) {
		args := []string{"api", "misc", "org-list-team-members",
			"--id", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListTeamRepo", func(t *testing.T) {
		args := []string{"api", "misc", "org-list-team-repo",
			"--id", "1",
			"--org", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListTeamRepos", func(t *testing.T) {
		args := []string{"api", "misc", "org-list-team-repos",
			"--id", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgRemoveTeamMember", func(t *testing.T) {
		args := []string{"api", "misc", "org-remove-team-member",
			"--id", "1",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgRemoveTeamRepository", func(t *testing.T) {
		args := []string{"api", "misc", "org-remove-team-repository",
			"--id", "1",
			"--org", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("registerUserRunner", func(t *testing.T) {
		args := []string{"api", "misc", "register-user-runner"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("renderMarkdown", func(t *testing.T) {
		args := []string{"api", "misc", "render-markdown"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("renderMarkdownRaw", func(t *testing.T) {
		args := []string{"api", "misc", "render-markdown-raw"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("renderMarkup", func(t *testing.T) {
		args := []string{"api", "misc", "render-markup"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoGetByID", func(t *testing.T) {
		args := []string{"api", "misc", "repo-get-by-i-d",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("topicSearch", func(t *testing.T) {
		args := []string{"api", "misc", "topic-search",
			"--q", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("unlinkPackage", func(t *testing.T) {
		args := []string{"api", "misc", "unlink-package",
			"--owner", testUser(),
			"--type", "1",
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("updateUserSecret", func(t *testing.T) {
		args := []string{"api", "misc", "update-user-secret",
			"--secretname", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("updateUserSettings", func(t *testing.T) {
		args := []string{"api", "misc", "update-user-settings"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("updateUserVariable", func(t *testing.T) {
		args := []string{"api", "misc", "update-user-variable",
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userAddEmail", func(t *testing.T) {
		args := []string{"api", "misc", "user-add-email"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userBlockUser", func(t *testing.T) {
		args := []string{"api", "misc", "user-block-user",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCheckQuota", func(t *testing.T) {
		args := []string{"api", "misc", "user-check-quota",
			"--subject", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCreateHook", func(t *testing.T) {
		args := []string{"api", "misc", "user-create-hook"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCreateOAuth2Application", func(t *testing.T) {
		args := []string{"api", "misc", "user-create-o-auth2-application"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentActivityPubFollow", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-activity-pub-follow"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentCheckFollowing", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-check-following",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentCheckStarring", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-check-starring",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentDeleteFollow", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-delete-follow",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentDeleteGPGKey", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-delete-g-p-g-key",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentDeleteKey", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-delete-key",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentDeleteStar", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-delete-star",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentGetGPGKey", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-get-g-p-g-key",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentGetKey", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-get-key",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentListFollowers", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-list-followers",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentListFollowing", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-list-following",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentListGPGKeys", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-list-g-p-g-keys",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentListKeys", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-list-keys",
			"--fingerprint", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentListRepos", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-list-repos",
			"--page", "1",
			"--limit", "1",
			"--order-by", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentListStarred", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-list-starred",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentListSubscriptions", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-list-subscriptions",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentPostGPGKey", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-post-g-p-g-key"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentPostKey", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-post-key"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentPutFollow", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-put-follow",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentPutStar", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-put-star",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentTrackedTimes", func(t *testing.T) {
		args := []string{"api", "misc", "user-current-tracked-times",
			"--page", "1",
			"--limit", "1",
			"--since", "1",
			"--before", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userDeleteAvatar", func(t *testing.T) {
		args := []string{"api", "misc", "user-delete-avatar"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userDeleteEmail", func(t *testing.T) {
		args := []string{"api", "misc", "user-delete-email"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userDeleteHook", func(t *testing.T) {
		args := []string{"api", "misc", "user-delete-hook",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userDeleteOAuth2Application", func(t *testing.T) {
		args := []string{"api", "misc", "user-delete-o-auth2-application",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userEditHook", func(t *testing.T) {
		args := []string{"api", "misc", "user-edit-hook",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userGetCurrent", func(t *testing.T) {
		args := []string{"api", "misc", "user-get-current"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetHook", func(t *testing.T) {
		args := []string{"api", "misc", "user-get-hook",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetOAuth2Application", func(t *testing.T) {
		args := []string{"api", "misc", "user-get-o-auth2-application",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetOAuth2Applications", func(t *testing.T) {
		args := []string{"api", "misc", "user-get-o-auth2-applications",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetQuota", func(t *testing.T) {
		args := []string{"api", "misc", "user-get-quota"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetRunnerRegistrationToken", func(t *testing.T) {
		args := []string{"api", "misc", "user-get-runner-registration-token"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetStopWatches", func(t *testing.T) {
		args := []string{"api", "misc", "user-get-stop-watches",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListBlockedUsers", func(t *testing.T) {
		args := []string{"api", "misc", "user-list-blocked-users",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListEmails", func(t *testing.T) {
		args := []string{"api", "misc", "user-list-emails"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListHooks", func(t *testing.T) {
		args := []string{"api", "misc", "user-list-hooks",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListQuotaArtifacts", func(t *testing.T) {
		args := []string{"api", "misc", "user-list-quota-artifacts",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListQuotaAttachments", func(t *testing.T) {
		args := []string{"api", "misc", "user-list-quota-attachments",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListQuotaPackages", func(t *testing.T) {
		args := []string{"api", "misc", "user-list-quota-packages",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListTeams", func(t *testing.T) {
		args := []string{"api", "misc", "user-list-teams",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userSearchRunJobs", func(t *testing.T) {
		args := []string{"api", "misc", "user-search-run-jobs",
			"--labels", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userUnblockUser", func(t *testing.T) {
		args := []string{"api", "misc", "user-unblock-user",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userUpdateAvatar", func(t *testing.T) {
		args := []string{"api", "misc", "user-update-avatar"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userUpdateOAuth2Application", func(t *testing.T) {
		args := []string{"api", "misc", "user-update-o-auth2-application",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userVerifyGPGKey", func(t *testing.T) {
		args := []string{"api", "misc", "user-verify-g-p-g-key"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
}

// TestGenerated_Notify tests all 5 auto-generated notify commands.
func TestGenerated_Notify(t *testing.T) {
	skipIfNoInstance(t)
	binary := buildFjBinary(t)
	t.Run("notifyGetList", func(t *testing.T) {
		args := []string{"api", "notify", "notify-get-list",
			"--all", "1",
			"--status-types", "1",
			"--subject-type", "1",
			"--since", "1",
			"--before", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("notifyGetThread", func(t *testing.T) {
		args := []string{"api", "notify", "notify-get-thread",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("notifyNewAvailable", func(t *testing.T) {
		args := []string{"api", "notify", "notify-new-available"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("notifyReadList", func(t *testing.T) {
		args := []string{"api", "notify", "notify-read-list",
			"--last-read-at", "1",
			"--all", "1",
			"--status-types", "1",
			"--to-status", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("notifyReadThread", func(t *testing.T) {
		args := []string{"api", "notify", "notify-read-thread",
			"--id", "1",
			"--to-status", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
}

// TestGenerated_Org tests all 53 auto-generated org commands.
func TestGenerated_Org(t *testing.T) {
	skipIfNoInstance(t)
	binary := buildFjBinary(t)
	t.Run("createOrgRepo", func(t *testing.T) {
		args := []string{"api", "org", "create-org-repo",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("createOrgVariable", func(t *testing.T) {
		args := []string{"api", "org", "create-org-variable",
			"--org", testUser(),
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteOrgRunner", func(t *testing.T) {
		args := []string{"api", "org", "delete-org-runner",
			"--org", testUser(),
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteOrgSecret", func(t *testing.T) {
		args := []string{"api", "org", "delete-org-secret",
			"--org", testUser(),
			"--secretname", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteOrgVariable", func(t *testing.T) {
		args := []string{"api", "org", "delete-org-variable",
			"--org", testUser(),
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("getOrgRunner", func(t *testing.T) {
		args := []string{"api", "org", "get-org-runner",
			"--org", testUser(),
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getOrgRunners", func(t *testing.T) {
		args := []string{"api", "org", "get-org-runners",
			"--org", testUser(),
			"--visible", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getOrgVariable", func(t *testing.T) {
		args := []string{"api", "org", "get-org-variable",
			"--org", testUser(),
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getOrgVariablesList", func(t *testing.T) {
		args := []string{"api", "org", "get-org-variables-list",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgBlockUser", func(t *testing.T) {
		args := []string{"api", "org", "org-block-user",
			"--org", testUser(),
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgCheckQuota", func(t *testing.T) {
		args := []string{"api", "org", "org-check-quota",
			"--org", testUser(),
			"--subject", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgConcealMember", func(t *testing.T) {
		args := []string{"api", "org", "org-conceal-member",
			"--org", testUser(),
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgCreate", func(t *testing.T) {
		args := []string{"api", "org", "org-create"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgCreateHook", func(t *testing.T) {
		args := []string{"api", "org", "org-create-hook",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgCreateLabel", func(t *testing.T) {
		args := []string{"api", "org", "org-create-label",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgCreateTeam", func(t *testing.T) {
		args := []string{"api", "org", "org-create-team",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgDelete", func(t *testing.T) {
		args := []string{"api", "org", "org-delete",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgDeleteAvatar", func(t *testing.T) {
		args := []string{"api", "org", "org-delete-avatar",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgDeleteHook", func(t *testing.T) {
		args := []string{"api", "org", "org-delete-hook",
			"--org", testUser(),
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgDeleteLabel", func(t *testing.T) {
		args := []string{"api", "org", "org-delete-label",
			"--org", testUser(),
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgDeleteMember", func(t *testing.T) {
		args := []string{"api", "org", "org-delete-member",
			"--org", testUser(),
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgEdit", func(t *testing.T) {
		args := []string{"api", "org", "org-edit",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgEditHook", func(t *testing.T) {
		args := []string{"api", "org", "org-edit-hook",
			"--org", testUser(),
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgEditLabel", func(t *testing.T) {
		args := []string{"api", "org", "org-edit-label",
			"--org", testUser(),
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgGet", func(t *testing.T) {
		args := []string{"api", "org", "org-get",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgGetAll", func(t *testing.T) {
		args := []string{"api", "org", "org-get-all",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgGetHook", func(t *testing.T) {
		args := []string{"api", "org", "org-get-hook",
			"--org", testUser(),
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgGetLabel", func(t *testing.T) {
		args := []string{"api", "org", "org-get-label",
			"--org", testUser(),
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgGetQuota", func(t *testing.T) {
		args := []string{"api", "org", "org-get-quota",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgGetRunnerRegistrationToken", func(t *testing.T) {
		args := []string{"api", "org", "org-get-runner-registration-token",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgIsMember", func(t *testing.T) {
		args := []string{"api", "org", "org-is-member",
			"--org", testUser(),
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgIsPublicMember", func(t *testing.T) {
		args := []string{"api", "org", "org-is-public-member",
			"--org", testUser(),
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgListActionsSecrets", func(t *testing.T) {
		args := []string{"api", "org", "org-list-actions-secrets",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListActivityFeeds", func(t *testing.T) {
		args := []string{"api", "org", "org-list-activity-feeds",
			"--org", testUser(),
			"--date", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListBlockedUsers", func(t *testing.T) {
		args := []string{"api", "org", "org-list-blocked-users",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListHooks", func(t *testing.T) {
		args := []string{"api", "org", "org-list-hooks",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListLabels", func(t *testing.T) {
		args := []string{"api", "org", "org-list-labels",
			"--org", testUser(),
			"--sort", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListMembers", func(t *testing.T) {
		args := []string{"api", "org", "org-list-members",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListPublicMembers", func(t *testing.T) {
		args := []string{"api", "org", "org-list-public-members",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListQuotaArtifacts", func(t *testing.T) {
		args := []string{"api", "org", "org-list-quota-artifacts",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListQuotaAttachments", func(t *testing.T) {
		args := []string{"api", "org", "org-list-quota-attachments",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListQuotaPackages", func(t *testing.T) {
		args := []string{"api", "org", "org-list-quota-packages",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListRepos", func(t *testing.T) {
		args := []string{"api", "org", "org-list-repos",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListTeams", func(t *testing.T) {
		args := []string{"api", "org", "org-list-teams",
			"--org", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgPublicizeMember", func(t *testing.T) {
		args := []string{"api", "org", "org-publicize-member",
			"--org", testUser(),
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgSearchRunJobs", func(t *testing.T) {
		args := []string{"api", "org", "org-search-run-jobs",
			"--org", testUser(),
			"--labels", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgUnblockUser", func(t *testing.T) {
		args := []string{"api", "org", "org-unblock-user",
			"--org", testUser(),
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("orgUpdateAvatar", func(t *testing.T) {
		args := []string{"api", "org", "org-update-avatar",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("registerOrgRunner", func(t *testing.T) {
		args := []string{"api", "org", "register-org-runner",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("renameOrg", func(t *testing.T) {
		args := []string{"api", "org", "rename-org",
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("teamSearch", func(t *testing.T) {
		args := []string{"api", "org", "team-search",
			"--org", testUser(),
			"--q", "1",
			"--include-desc", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("updateOrgSecret", func(t *testing.T) {
		args := []string{"api", "org", "update-org-secret",
			"--org", testUser(),
			"--secretname", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("updateOrgVariable", func(t *testing.T) {
		args := []string{"api", "org", "update-org-variable",
			"--org", testUser(),
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
}

// TestGenerated_Repo tests all 264 auto-generated repo commands.
func TestGenerated_Repo(t *testing.T) {
	skipIfNoInstance(t)
	binary := buildFjBinary(t)
	t.Run("ActionRun", func(t *testing.T) {
		args := []string{"api", "repo", "action-run",
			"--owner", testUser(),
			"--repo", testRepo,
			"--run-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("CancelActionRun", func(t *testing.T) {
		args := []string{"api", "repo", "cancel-action-run",
			"--owner", testUser(),
			"--repo", testRepo,
			"--run-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("DeleteActionArtifact", func(t *testing.T) {
		args := []string{"api", "repo", "delete-action-artifact",
			"--owner", testUser(),
			"--repo", testRepo,
			"--artifact-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("DeleteActionRun", func(t *testing.T) {
		args := []string{"api", "repo", "delete-action-run",
			"--owner", testUser(),
			"--repo", testRepo,
			"--run-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("DispatchWorkflow", func(t *testing.T) {
		args := []string{"api", "repo", "dispatch-workflow",
			"--owner", testUser(),
			"--repo", testRepo,
			"--workflowfilename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("DownloadActionArtifact", func(t *testing.T) {
		args := []string{"api", "repo", "download-action-artifact",
			"--owner", testUser(),
			"--repo", testRepo,
			"--artifact-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("GetActionArtifact", func(t *testing.T) {
		args := []string{"api", "repo", "get-action-artifact",
			"--owner", testUser(),
			"--repo", testRepo,
			"--artifact-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("GetAnnotatedTag", func(t *testing.T) {
		args := []string{"api", "repo", "get-annotated-tag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("GetBlob", func(t *testing.T) {
		args := []string{"api", "repo", "get-blob",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("GetBlobs", func(t *testing.T) {
		args := []string{"api", "repo", "get-blobs",
			"--owner", testUser(),
			"--repo", testRepo,
			"--shas", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("GetTree", func(t *testing.T) {
		args := []string{"api", "repo", "get-tree",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
			"--recursive", "1",
			"--page", "1",
			"--per-page", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("ListActionArtifacts", func(t *testing.T) {
		args := []string{"api", "repo", "list-action-artifacts",
			"--owner", testUser(),
			"--repo", testRepo,
			"--name", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("ListActionRunArtifacts", func(t *testing.T) {
		args := []string{"api", "repo", "list-action-run-artifacts",
			"--owner", testUser(),
			"--repo", testRepo,
			"--run-id", "1",
			"--name", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("ListActionRunJobs", func(t *testing.T) {
		args := []string{"api", "repo", "list-action-run-jobs",
			"--owner", testUser(),
			"--repo", testRepo,
			"--run-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("ListActionRuns", func(t *testing.T) {
		args := []string{"api", "repo", "list-action-runs",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
			"--event", "1",
			"--status", "1",
			"--run-number", "1",
			"--head-sha", "1",
			"--ref", "1",
			"--workflow-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("ListActionTasks", func(t *testing.T) {
		args := []string{"api", "repo", "list-action-tasks",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
			"--status", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("acceptRepoTransfer", func(t *testing.T) {
		args := []string{"api", "repo", "accept-repo-transfer",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("createFork", func(t *testing.T) {
		args := []string{"api", "repo", "create-fork",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("createRepoVariable", func(t *testing.T) {
		args := []string{"api", "repo", "create-repo-variable",
			"--owner", testUser(),
			"--repo", testRepo,
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteRepoRunner", func(t *testing.T) {
		args := []string{"api", "repo", "delete-repo-runner",
			"--owner", testUser(),
			"--repo", testRepo,
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteRepoSecret", func(t *testing.T) {
		args := []string{"api", "repo", "delete-repo-secret",
			"--owner", testUser(),
			"--repo", testRepo,
			"--secretname", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("deleteRepoVariable", func(t *testing.T) {
		args := []string{"api", "repo", "delete-repo-variable",
			"--owner", testUser(),
			"--repo", testRepo,
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("generateRepo", func(t *testing.T) {
		args := []string{"api", "repo", "generate-repo",
			"--template-owner", "1",
			"--template-repo", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("getRepoRunner", func(t *testing.T) {
		args := []string{"api", "repo", "get-repo-runner",
			"--owner", testUser(),
			"--repo", testRepo,
			"--runner-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getRepoRunners", func(t *testing.T) {
		args := []string{"api", "repo", "get-repo-runners",
			"--owner", testUser(),
			"--repo", testRepo,
			"--visible", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getRepoVariable", func(t *testing.T) {
		args := []string{"api", "repo", "get-repo-variable",
			"--owner", testUser(),
			"--repo", testRepo,
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("getRepoVariablesList", func(t *testing.T) {
		args := []string{"api", "repo", "get-repo-variables-list",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueAddLabel", func(t *testing.T) {
		args := []string{"api", "repo", "issue-add-label",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueAddSubscription", func(t *testing.T) {
		args := []string{"api", "repo", "issue-add-subscription",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--user", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueAddTime", func(t *testing.T) {
		args := []string{"api", "repo", "issue-add-time",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCheckSubscription", func(t *testing.T) {
		args := []string{"api", "repo", "issue-check-subscription",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueClearLabels", func(t *testing.T) {
		args := []string{"api", "repo", "issue-clear-labels",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateComment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-comment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateIssue", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-issue",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateIssueAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-issue-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--name", "1",
			"--updated-at", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateIssueBlocking", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-issue-blocking",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateIssueCommentAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-issue-comment-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--name", "1",
			"--updated-at", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateIssueDependencies", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-issue-dependencies",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateLabel", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-label",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueCreateMilestone", func(t *testing.T) {
		args := []string{"api", "repo", "issue-create-milestone",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDelete", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteComment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-comment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteCommentDeprecated", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-comment-deprecated",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteCommentReaction", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-comment-reaction",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteIssueAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-issue-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteIssueCommentAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-issue-comment-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteIssueReaction", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-issue-reaction",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteLabel", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-label",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteMilestone", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-milestone",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteStopWatch", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-stop-watch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteSubscription", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-subscription",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--user", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueDeleteTime", func(t *testing.T) {
		args := []string{"api", "repo", "issue-delete-time",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditComment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-comment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditCommentDeprecated", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-comment-deprecated",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditIssue", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-issue",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditIssueAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-issue-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditIssueCommentAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-issue-comment-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditIssueDeadline", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-issue-deadline",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditLabel", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-label",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueEditMilestone", func(t *testing.T) {
		args := []string{"api", "repo", "issue-edit-milestone",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueGetComment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-comment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetCommentReactions", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-comment-reactions",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetComments", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-comments",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--since", "1",
			"--before", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetCommentsAndTimeline", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-comments-and-timeline",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--since", "1",
			"--page", "1",
			"--limit", "1",
			"--before", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetIssue", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-issue",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetIssueAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-issue-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetIssueCommentAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-issue-comment-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetIssueReactions", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-issue-reactions",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetLabel", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-label",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetLabels", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-labels",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetMilestone", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-milestone",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetMilestonesList", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-milestones-list",
			"--owner", testUser(),
			"--repo", testRepo,
			"--state", "1",
			"--name", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueGetRepoComments", func(t *testing.T) {
		args := []string{"api", "repo", "issue-get-repo-comments",
			"--owner", testUser(),
			"--repo", testRepo,
			"--since", "1",
			"--before", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueListBlocks", func(t *testing.T) {
		args := []string{"api", "repo", "issue-list-blocks",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueListIssueAttachments", func(t *testing.T) {
		args := []string{"api", "repo", "issue-list-issue-attachments",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueListIssueCommentAttachments", func(t *testing.T) {
		args := []string{"api", "repo", "issue-list-issue-comment-attachments",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueListIssueDependencies", func(t *testing.T) {
		args := []string{"api", "repo", "issue-list-issue-dependencies",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueListIssues", func(t *testing.T) {
		args := []string{"api", "repo", "issue-list-issues",
			"--owner", testUser(),
			"--repo", testRepo,
			"--state", "1",
			"--labels", "1",
			"--q", "1",
			"--type", "1",
			"--milestones", "1",
			"--since", "1",
			"--before", "1",
			"--created-by", "1",
			"--assigned-by", "1",
			"--mentioned-by", "1",
			"--page", "1",
			"--limit", "1",
			"--sort", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueListLabels", func(t *testing.T) {
		args := []string{"api", "repo", "issue-list-labels",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sort", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issuePostCommentReaction", func(t *testing.T) {
		args := []string{"api", "repo", "issue-post-comment-reaction",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issuePostIssueReaction", func(t *testing.T) {
		args := []string{"api", "repo", "issue-post-issue-reaction",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueRemoveIssueBlocking", func(t *testing.T) {
		args := []string{"api", "repo", "issue-remove-issue-blocking",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueRemoveIssueDependencies", func(t *testing.T) {
		args := []string{"api", "repo", "issue-remove-issue-dependencies",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueRemoveLabel", func(t *testing.T) {
		args := []string{"api", "repo", "issue-remove-label",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--identifier", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueReplaceLabels", func(t *testing.T) {
		args := []string{"api", "repo", "issue-replace-labels",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueResetTime", func(t *testing.T) {
		args := []string{"api", "repo", "issue-reset-time",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueSearchIssues", func(t *testing.T) {
		args := []string{"api", "repo", "issue-search-issues",
			"--state", "1",
			"--labels", "1",
			"--milestones", "1",
			"--q", "1",
			"--priority-repo-id", "1",
			"--type", "1",
			"--since", "1",
			"--before", "1",
			"--assigned", "1",
			"--created", "1",
			"--mentioned", "1",
			"--review-requested", "1",
			"--reviewed", "1",
			"--owner", testUser(),
			"--team", "1",
			"--page", "1",
			"--limit", "1",
			"--sort", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueStartStopWatch", func(t *testing.T) {
		args := []string{"api", "repo", "issue-start-stop-watch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueStopStopWatch", func(t *testing.T) {
		args := []string{"api", "repo", "issue-stop-stop-watch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("issueSubscriptions", func(t *testing.T) {
		args := []string{"api", "repo", "issue-subscriptions",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("issueTrackedTimes", func(t *testing.T) {
		args := []string{"api", "repo", "issue-tracked-times",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--user", "1",
			"--since", "1",
			"--before", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("listForks", func(t *testing.T) {
		args := []string{"api", "repo", "list-forks",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("moveIssuePin", func(t *testing.T) {
		args := []string{"api", "repo", "move-issue-pin",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--position", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("notifyGetRepoList", func(t *testing.T) {
		args := []string{"api", "repo", "notify-get-repo-list",
			"--owner", testUser(),
			"--repo", testRepo,
			"--all", "1",
			"--status-types", "1",
			"--subject-type", "1",
			"--since", "1",
			"--before", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("notifyReadRepoList", func(t *testing.T) {
		args := []string{"api", "repo", "notify-read-repo-list",
			"--owner", testUser(),
			"--repo", testRepo,
			"--all", "1",
			"--status-types", "1",
			"--to-status", "1",
			"--last-read-at", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("pinIssue", func(t *testing.T) {
		args := []string{"api", "repo", "pin-issue",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("registerRepoRunner", func(t *testing.T) {
		args := []string{"api", "repo", "register-repo-runner",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("rejectRepoTransfer", func(t *testing.T) {
		args := []string{"api", "repo", "reject-repo-transfer",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoAddCollaborator", func(t *testing.T) {
		args := []string{"api", "repo", "repo-add-collaborator",
			"--owner", testUser(),
			"--repo", testRepo,
			"--collaborator", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoAddFlag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-add-flag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--flag", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoAddPushMirror", func(t *testing.T) {
		args := []string{"api", "repo", "repo-add-push-mirror",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoAddTeam", func(t *testing.T) {
		args := []string{"api", "repo", "repo-add-team",
			"--owner", testUser(),
			"--repo", testRepo,
			"--team", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoAddTopic", func(t *testing.T) {
		args := []string{"api", "repo", "repo-add-topic",
			"--owner", testUser(),
			"--repo", testRepo,
			"--topic", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoApplyDiffPatch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-apply-diff-patch",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCancelScheduledAutoMerge", func(t *testing.T) {
		args := []string{"api", "repo", "repo-cancel-scheduled-auto-merge",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoChangeFiles", func(t *testing.T) {
		args := []string{"api", "repo", "repo-change-files",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCheckCollaborator", func(t *testing.T) {
		args := []string{"api", "repo", "repo-check-collaborator",
			"--owner", testUser(),
			"--repo", testRepo,
			"--collaborator", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCheckFlag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-check-flag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--flag", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCheckTeam", func(t *testing.T) {
		args := []string{"api", "repo", "repo-check-team",
			"--owner", testUser(),
			"--repo", testRepo,
			"--team", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoCompareDiff", func(t *testing.T) {
		args := []string{"api", "repo", "repo-compare-diff",
			"--owner", testUser(),
			"--repo", testRepo,
			"--basehead", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoConvert", func(t *testing.T) {
		args := []string{"api", "repo", "repo-convert",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateBranch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-branch",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateBranchProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-branch-protection",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateFile", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-file",
			"--owner", testUser(),
			"--repo", testRepo,
			"--filepath", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-hook",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateKey", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-key",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreatePullRequest", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-pull-request",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreatePullReview", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-pull-review",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreatePullReviewComment", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-pull-review-comment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreatePullReviewRequests", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-pull-review-requests",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateRelease", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-release",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateReleaseAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-release-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateStatus", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-status",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateTag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-tag",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateTagProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-tag-protection",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoCreateWikiPage", func(t *testing.T) {
		args := []string{"api", "repo", "repo-create-wiki-page",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDelete", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteAllFlags", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-all-flags",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteAvatar", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-avatar",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteBranch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-branch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--branch", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteBranchProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-branch-protection",
			"--owner", testUser(),
			"--repo", testRepo,
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteCollaborator", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-collaborator",
			"--owner", testUser(),
			"--repo", testRepo,
			"--collaborator", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteFile", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-file",
			"--owner", testUser(),
			"--repo", testRepo,
			"--filepath", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteFlag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-flag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--flag", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteGitHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-git-hook",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-hook",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteKey", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-key",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeletePullReview", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-pull-review",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeletePullReviewComment", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-pull-review-comment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
			"--comment", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeletePullReviewRequests", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-pull-review-requests",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeletePushMirror", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-push-mirror",
			"--owner", testUser(),
			"--repo", testRepo,
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteRelease", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-release",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteReleaseAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-release-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteReleaseByTag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-release-by-tag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--tag", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteTag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-tag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--tag", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteTagProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-tag-protection",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteTeam", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-team",
			"--owner", testUser(),
			"--repo", testRepo,
			"--team", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteTopic", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-topic",
			"--owner", testUser(),
			"--repo", testRepo,
			"--topic", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDeleteWikiPage", func(t *testing.T) {
		args := []string{"api", "repo", "repo-delete-wiki-page",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page-name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDismissPullReview", func(t *testing.T) {
		args := []string{"api", "repo", "repo-dismiss-pull-review",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoDownloadCommitDiffOrPatch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-download-commit-diff-or-patch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
			"--diff-type", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoDownloadPullDiffOrPatch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-download-pull-diff-or-patch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--diff-type", "1",
			"--binary", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoEdit", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditBranchProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-branch-protection",
			"--owner", testUser(),
			"--repo", testRepo,
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditGitHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-git-hook",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-hook",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditPullRequest", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-pull-request",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditRelease", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-release",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditReleaseAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-release-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditTagProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-tag-protection",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoEditWikiPage", func(t *testing.T) {
		args := []string{"api", "repo", "repo-edit-wiki-page",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page-name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoGet", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetActionJobLogs", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-action-job-logs",
			"--owner", testUser(),
			"--repo", testRepo,
			"--job-id", "1",
			"--attempt", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetActionRunLogs", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-action-run-logs",
			"--owner", testUser(),
			"--repo", testRepo,
			"--run-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetAllCommits", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-all-commits",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
			"--path", "1",
			"--stat", "1",
			"--verification", "1",
			"--files", "1",
			"--page", "1",
			"--limit", "1",
			"--not", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetArchive", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-archive",
			"--owner", testUser(),
			"--repo", testRepo,
			"--archive", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoGetAssignees", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-assignees",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetBranch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-branch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--branch", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetBranchProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-branch-protection",
			"--owner", testUser(),
			"--repo", testRepo,
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetCombinedStatusByRef", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-combined-status-by-ref",
			"--owner", testUser(),
			"--repo", testRepo,
			"--ref", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetCommitPullRequest", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-commit-pull-request",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetContents", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-contents",
			"--owner", testUser(),
			"--repo", testRepo,
			"--filepath", "1",
			"--ref", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetContentsList", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-contents-list",
			"--owner", testUser(),
			"--repo", testRepo,
			"--ref", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetEditorConfig", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-editor-config",
			"--owner", testUser(),
			"--repo", testRepo,
			"--filepath", "1",
			"--ref", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetGitHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-git-hook",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-hook",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetIssueConfig", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-issue-config",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetIssueTemplates", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-issue-templates",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetKey", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-key",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetLanguages", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-languages",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetLatestRelease", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-latest-release",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetNote", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-note",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
			"--verification", "1",
			"--files", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPullRequest", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-pull-request",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPullRequestByBaseHead", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-pull-request-by-base-head",
			"--owner", testUser(),
			"--repo", testRepo,
			"--base", "1",
			"--head", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPullRequestCommits", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-pull-request-commits",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--page", "1",
			"--limit", "1",
			"--verification", "1",
			"--files", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPullRequestFiles", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-pull-request-files",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--skip-to", "1",
			"--whitespace", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPullReview", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-pull-review",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPullReviewComment", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-pull-review-comment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
			"--comment", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPullReviewComments", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-pull-review-comments",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetPushMirrorByRemoteName", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-push-mirror-by-remote-name",
			"--owner", testUser(),
			"--repo", testRepo,
			"--name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetRawFile", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-raw-file",
			"--owner", testUser(),
			"--repo", testRepo,
			"--filepath", "1",
			"--ref", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetRawFileOrLFS", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-raw-file-or-l-f-s",
			"--owner", testUser(),
			"--repo", testRepo,
			"--filepath", "1",
			"--ref", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetRelease", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-release",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetReleaseAttachment", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-release-attachment",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--attachment-id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetReleaseByTag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-release-by-tag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--tag", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetRepoPermissions", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-repo-permissions",
			"--owner", testUser(),
			"--repo", testRepo,
			"--collaborator", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetReviewers", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-reviewers",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetRunnerRegistrationToken", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-runner-registration-token",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetSingleCommit", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-single-commit",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
			"--stat", "1",
			"--verification", "1",
			"--files", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetTag", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-tag",
			"--owner", testUser(),
			"--repo", testRepo,
			"--tag", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetTagProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-tag-protection",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetWikiPage", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-wiki-page",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page-name", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetWikiPageRevisions", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-wiki-page-revisions",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page-name", "1",
			"--page", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoGetWikiPages", func(t *testing.T) {
		args := []string{"api", "repo", "repo-get-wiki-pages",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListActionsSecrets", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-actions-secrets",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListActivityFeeds", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-activity-feeds",
			"--owner", testUser(),
			"--repo", testRepo,
			"--date", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListAllGitRefs", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-all-git-refs",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListBranchProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-branch-protection",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListBranches", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-branches",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListCollaborators", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-collaborators",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListFlags", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-flags",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListGitHooks", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-git-hooks",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListGitRefs", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-git-refs",
			"--owner", testUser(),
			"--repo", testRepo,
			"--ref", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListHooks", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-hooks",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListKeys", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-keys",
			"--owner", testUser(),
			"--repo", testRepo,
			"--key-id", "1",
			"--fingerprint", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListPinnedIssues", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-pinned-issues",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListPinnedPullRequests", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-pinned-pull-requests",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListPullRequests", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-pull-requests",
			"--owner", testUser(),
			"--repo", testRepo,
			"--state", "1",
			"--sort", "1",
			"--milestone", "1",
			"--labels", "1",
			"--poster", "1",
			"--base", "1",
			"--head", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListPullReviews", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-pull-reviews",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListPushMirrors", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-push-mirrors",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListReleaseAttachments", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-release-attachments",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListReleases", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-releases",
			"--owner", testUser(),
			"--repo", testRepo,
			"--draft", "1",
			"--pre-release", "1",
			"--q", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListStargazers", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-stargazers",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListStatuses", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-statuses",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
			"--sort", "1",
			"--state", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListStatusesByRef", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-statuses-by-ref",
			"--owner", testUser(),
			"--repo", testRepo,
			"--ref", "1",
			"--sort", "1",
			"--state", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListSubscribers", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-subscribers",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListTagProtection", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-tag-protection",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListTags", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-tags",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListTeams", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-teams",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoListTopics", func(t *testing.T) {
		args := []string{"api", "repo", "repo-list-topics",
			"--owner", testUser(),
			"--repo", testRepo,
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoMergePullRequest", func(t *testing.T) {
		args := []string{"api", "repo", "repo-merge-pull-request",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoMigrate", func(t *testing.T) {
		args := []string{"api", "repo", "repo-migrate"}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoMirrorSync", func(t *testing.T) {
		args := []string{"api", "repo", "repo-mirror-sync",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoNewPinAllowed", func(t *testing.T) {
		args := []string{"api", "repo", "repo-new-pin-allowed",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoPullRequestIsMerged", func(t *testing.T) {
		args := []string{"api", "repo", "repo-pull-request-is-merged",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoPushMirrorSync", func(t *testing.T) {
		args := []string{"api", "repo", "repo-push-mirror-sync",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoRemoveNote", func(t *testing.T) {
		args := []string{"api", "repo", "repo-remove-note",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoReplaceAllFlags", func(t *testing.T) {
		args := []string{"api", "repo", "repo-replace-all-flags",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoSearch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-search",
			"--q", "1",
			"--topic", "1",
			"--include-desc", "1",
			"--uid", "1",
			"--priority-owner-id", "1",
			"--team-id", "1",
			"--starred-by", "1",
			"--private", "1",
			"--is-private", "1",
			"--template", "1",
			"--archived", "1",
			"--mode", "1",
			"--exclusive", "1",
			"--sort", "1",
			"--order", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoSearchRunJobs", func(t *testing.T) {
		args := []string{"api", "repo", "repo-search-run-jobs",
			"--owner", testUser(),
			"--repo", testRepo,
			"--labels", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoSetNote", func(t *testing.T) {
		args := []string{"api", "repo", "repo-set-note",
			"--owner", testUser(),
			"--repo", testRepo,
			"--sha", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoSigningKey", func(t *testing.T) {
		args := []string{"api", "repo", "repo-signing-key",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoSubmitPullReview", func(t *testing.T) {
		args := []string{"api", "repo", "repo-submit-pull-review",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoSyncForkBranch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-sync-fork-branch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--branch", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoSyncForkBranchInfo", func(t *testing.T) {
		args := []string{"api", "repo", "repo-sync-fork-branch-info",
			"--owner", testUser(),
			"--repo", testRepo,
			"--branch", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoSyncForkDefault", func(t *testing.T) {
		args := []string{"api", "repo", "repo-sync-fork-default",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoSyncForkDefaultInfo", func(t *testing.T) {
		args := []string{"api", "repo", "repo-sync-fork-default-info",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoTestHook", func(t *testing.T) {
		args := []string{"api", "repo", "repo-test-hook",
			"--owner", testUser(),
			"--repo", testRepo,
			"--id", "1",
			"--ref", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoTrackedTimes", func(t *testing.T) {
		args := []string{"api", "repo", "repo-tracked-times",
			"--owner", testUser(),
			"--repo", testRepo,
			"--user", "1",
			"--since", "1",
			"--before", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("repoTransfer", func(t *testing.T) {
		args := []string{"api", "repo", "repo-transfer",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoUnDismissPullReview", func(t *testing.T) {
		args := []string{"api", "repo", "repo-un-dismiss-pull-review",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--id", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoUpdateAvatar", func(t *testing.T) {
		args := []string{"api", "repo", "repo-update-avatar",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoUpdateBranch", func(t *testing.T) {
		args := []string{"api", "repo", "repo-update-branch",
			"--owner", testUser(),
			"--repo", testRepo,
			"--branch", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoUpdateFile", func(t *testing.T) {
		args := []string{"api", "repo", "repo-update-file",
			"--owner", testUser(),
			"--repo", testRepo,
			"--filepath", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoUpdatePullRequest", func(t *testing.T) {
		args := []string{"api", "repo", "repo-update-pull-request",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
			"--style", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoUpdateTopics", func(t *testing.T) {
		args := []string{"api", "repo", "repo-update-topics",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("repoValidateIssueConfig", func(t *testing.T) {
		args := []string{"api", "repo", "repo-validate-issue-config",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("unpinIssue", func(t *testing.T) {
		args := []string{"api", "repo", "unpin-issue",
			"--owner", testUser(),
			"--repo", testRepo,
			"--index", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("updateRepoSecret", func(t *testing.T) {
		args := []string{"api", "repo", "update-repo-secret",
			"--owner", testUser(),
			"--repo", testRepo,
			"--secretname", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("updateRepoVariable", func(t *testing.T) {
		args := []string{"api", "repo", "update-repo-variable",
			"--owner", testUser(),
			"--repo", testRepo,
			"--variablename", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentCheckSubscription", func(t *testing.T) {
		args := []string{"api", "repo", "user-current-check-subscription",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCurrentDeleteSubscription", func(t *testing.T) {
		args := []string{"api", "repo", "user-current-delete-subscription",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCurrentPutSubscription", func(t *testing.T) {
		args := []string{"api", "repo", "user-current-put-subscription",
			"--owner", testUser(),
			"--repo", testRepo,
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userTrackedTimes", func(t *testing.T) {
		args := []string{"api", "repo", "user-tracked-times",
			"--owner", testUser(),
			"--repo", testRepo,
			"--user", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
}

// TestGenerated_User tests all 17 auto-generated user commands.
func TestGenerated_User(t *testing.T) {
	skipIfNoInstance(t)
	binary := buildFjBinary(t)
	t.Run("orgGetUserPermissions", func(t *testing.T) {
		args := []string{"api", "user", "org-get-user-permissions",
			"--username", testUser(),
			"--org", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("orgListUserOrgs", func(t *testing.T) {
		args := []string{"api", "user", "org-list-user-orgs",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userCheckFollowing", func(t *testing.T) {
		args := []string{"api", "user", "user-check-following",
			"--username", testUser(),
			"--target", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userCreateToken", func(t *testing.T) {
		args := []string{"api", "user", "user-create-token",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userDeleteAccessToken", func(t *testing.T) {
		args := []string{"api", "user", "user-delete-access-token",
			"--username", testUser(),
			"--token", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// Write commands: accept any HTTP error (404/403/409/422)
			if isAcceptableError(out) { t.Skip("endpoint exists, write skipped (no body)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
	})
	t.Run("userGet", func(t *testing.T) {
		args := []string{"api", "user", "user-get",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetHeatmapData", func(t *testing.T) {
		args := []string{"api", "user", "user-get-heatmap-data",
			"--username", testUser(),
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userGetTokens", func(t *testing.T) {
		args := []string{"api", "user", "user-get-tokens",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListActivityFeeds", func(t *testing.T) {
		args := []string{"api", "user", "user-list-activity-feeds",
			"--username", testUser(),
			"--only-performed-by", "1",
			"--date", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListFollowers", func(t *testing.T) {
		args := []string{"api", "user", "user-list-followers",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListFollowing", func(t *testing.T) {
		args := []string{"api", "user", "user-list-following",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListGPGKeys", func(t *testing.T) {
		args := []string{"api", "user", "user-list-g-p-g-keys",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListKeys", func(t *testing.T) {
		args := []string{"api", "user", "user-list-keys",
			"--username", testUser(),
			"--fingerprint", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListRepos", func(t *testing.T) {
		args := []string{"api", "user", "user-list-repos",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListStarred", func(t *testing.T) {
		args := []string{"api", "user", "user-list-starred",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userListSubscriptions", func(t *testing.T) {
		args := []string{"api", "user", "user-list-subscriptions",
			"--username", testUser(),
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
	t.Run("userSearch", func(t *testing.T) {
		args := []string{"api", "user", "user-search",
			"--q", "1",
			"--uid", "1",
			"--sort", "1",
			"--page", "1",
			"--limit", "1",
		}
		out, err := runFj(t, binary, args...)
		if err != nil {
			// GET: 404/403 means resource doesn't exist — still proves the command works
			if isAcceptableError(out) { t.Skip("resource not found (command works)") }
			t.Errorf("%v\n%s", err, out)
			return
		}
		// Verify valid JSON output
		trimmed := bytes.TrimSpace([]byte(out))
		if len(trimmed) > 0 {
			var v interface{}
			if e := json.Unmarshal(trimmed, &v); e != nil {
				t.Errorf("invalid JSON: %v\n%s", e, string(trimmed[:min(len(trimmed),200)]))
			}
		}
	})
}

// setupTestRepo creates a shared test repo for repo-scoped commands.
// Call from TestMain or the first test that needs it.
func setupTestRepo(t *testing.T) {
	t.Helper()
	if testRepo != "" { return }
	testRepo = uniqueRepoName("gen")
	createTestRepo(t, testRepo)
}

