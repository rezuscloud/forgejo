name: test
jobs:
  reusable-1:
    uses: ./.forgejo/workflows/expand_reusable_outputs_reusable-1.yml
  finalize:
    needs: [reusable-1]
    runs-on: ubuntu
    steps:
      - run: echo ${{ needs.reusable-1.outputs.output-1 }}
