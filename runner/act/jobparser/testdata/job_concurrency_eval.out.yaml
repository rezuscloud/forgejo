name: test
jobs:
  job1:
    name: job1
    runs-on: linux
    steps:
      - run: uname -a
    concurrency:
      group: ${{ github.workflow }}
      cancel-in-progress: ${{ !contains(github.ref, 'release/')}}
