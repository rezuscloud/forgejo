on:
  push:
jobs:
  test:
    runs-on: irrelevant
    steps:
      - run: |
          echo "forgejo.ref=${{ forgejo.ref }}"
          [[ "${{ forgejo.ref }}" =~ ^refs/(heads|remotes|rewritten|tags)/.+$ ]] || exit 1
